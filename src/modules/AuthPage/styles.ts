import { createStyles } from "@mantine/core";

export const useStyles = createStyles((theme) => ({
  brandButton: {
    marginTop: "30px",
    marginLeft: "auto",
    marginRight: "auto",
    cursor: "pointer",
  },
  facebookButton: {},
  googleButton: {},
}));
